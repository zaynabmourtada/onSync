import 'package:flutter/material.dart';

class CoffeeMachineScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Coffee Machine'),
      ),
      body: Center(
        child: Text('This is the Coffee Machine screen'),
      ),
    );
  }
}
